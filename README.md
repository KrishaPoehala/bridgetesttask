To run this add postgres connection string to your using setttins in format like this:
```
{
  "ConnectionStrings:Default": "Server=localhost; Port=5433; User Id=postgres; Password=*****; Database=dogsdb"
}
```
